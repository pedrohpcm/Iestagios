# PDS2 - Mobile Device

```
Version of the Ionic used in this project is 3.9.2.
If you want to know your version run the following command:

$ ionic --version or ionic -v
```

```
You can downgrade to this version by running:

$ npm install -g ionic@3.9.2
```

```
Version of the Node.js used in this project is 6.11.3.
If you want to know your version run the following command:

$ node --version or node -v
```

```
Version of the NPM (Node Package Manager) used in this project is 3.10.10.
If you want to know your version run the following command:

$ npm --version or npm -v
```